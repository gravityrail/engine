<div>
  <h2>Videos</h2>
  
  <%= link_to "New Video", new_video_path %><br><br>
  <% Locomotive::Video.all.each do |video| %>
    <%= link_to "Video ##{video.id}", video_path(video) %><br>
  <% end %>
</div>